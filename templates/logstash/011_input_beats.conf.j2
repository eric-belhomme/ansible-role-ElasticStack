input {
  beats {
    port => {{ ls_beats_listen_port }}
{% if ls_ssl_certificate_file and ls_ssl_key_file %}
    ssl => true
    ssl_certificate => "{{ ls_ssl_certificate_file }}"
    ssl_key => "{{ ls_ssl_key_file }}"
    ssl_verify_mode => "force_peer"
{% endif %}
  }
}
